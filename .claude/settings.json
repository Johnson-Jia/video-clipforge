{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "d=$PWD; while [ \"$d\" != \"/\" ]; do f=\"$d/.claude/commands/clipforge/scripts/rm_guard.py\"; if [ -f \"$f\" ]; then python \"$f\"; exit $?; fi; d=$(dirname \"$d\"); done; exit 0"
          }
        ]
      }
    ]
  }
}
